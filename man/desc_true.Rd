% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc_true_function.R
\name{desc_true}
\alias{desc_true}
\title{Create a Summary Table for Descriptive Statistics}
\usage{
desc_true(
  data1,
  table_title = "",
  quali = NULL,
  quanti = NULL,
  round_quanti = c(1),
  round_quali = c(0, 1),
  var_title = "Variable",
  var_group = NULL,
  group_title = ""
)
}
\arguments{
\item{data1}{A data frame containing the dataset to summarize.}

\item{table_title}{A character string specifying the title of the table (default: \code{""}).}

\item{quali}{A vector of qualitative variables to include in the summary. These should be poorly described variables to prioritize for better description (default: \code{NULL}).}

\item{quanti}{A vector of quantitative variables to include in the summary (default: \code{NULL}).}

\item{var_title}{A character string specifying the column header for variables in the table (default: \code{"Variable"}).}

\item{var_group}{A grouping variable to segment the table by. It must be a column in \code{data1} (default: \code{NULL}).}

\item{group_title}{A character string specifying the header for the group variable in the table (default: \code{""}).}
}
\value{
A \code{gtsummary} object representing the formatted summary table.
}
\description{
This function generates a summary table of descriptive statistics for the given dataset,
tailored for both categorical and continuous variables. It allows grouping and provides
options to customize table titles, variable labels, and grouping headers.
}
\details{
The function uses the \code{gtsummary} package to generate descriptive statistics.
\itemize{
\item Categorical variables are summarized using counts and percentages.
\item Continuous variables are summarized with the mean, standard deviation, minimum, maximum, median, and interquartile range.
\item Missing data statistics are added as a separate column, and formatting is applied to emphasize key elements (e.g., bold variable labels, italicized levels).
}

It supports additional customization options such as hiding NA values, controlling the number of decimal places, and adding headers or captions.
}
\examples{
# Example usage:
library(gtsummary)
data(mtcars)
desc_table <- desc_true(
  data1 = mtcars,
  table_title = "Descriptive Statistics of mtcars Dataset",
  quali = c("cyl", "gear"),
  quanti = c("mpg", "disp", "hp"),
  var_title = "Variable Name",
  var_group = "cyl",
  group_title = "Cylinders"
)

desc_table

}
