% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desc_all_function.R
\name{desc_all}
\alias{desc_all}
\title{Create a Descriptive Summary Table}
\usage{
desc_all(
  data1,
  table_title = "",
  quali = NULL,
  quanti = NULL,
  round_quanti = c(1),
  round_quali = c(0, 1),
  var_title = "Variable",
  var_group = NULL,
  group_title = ""
)
}
\arguments{
\item{data1}{A data frame containing the variables to summarize.}

\item{table_title}{A string specifying the title of the table. Defaults to an empty string.}

\item{quali}{A vector of variable names (as strings) that should be treated as qualitative.}

\item{quanti}{A vector of variable names (as strings) that should be treated as quantitative.}

\item{var_title}{A string specifying the column label for variables in the summary table. Defaults to "Variable".}

\item{var_group}{A grouping variable (unquoted) to generate group-wise summaries. If \code{NULL}, no grouping is applied.}

\item{group_title}{A string specifying the column label for the grouping variable in the table. Defaults to an empty string.}
}
\value{
A \code{gtsummary} object representing the formatted summary table.
}
\description{
Generates a summary table of descriptive statistics for quantitative and qualitative variables,
grouped by a specific variable if provided.
}
\details{
This function leverages the \code{gtsummary} package to create a highly customizable summary table.
Quantitative variables are summarized with mean (standard deviation), median (interquartile range),
and range (minimum, maximum). Qualitative variables are summarized with counts and percentages.
The table includes options for overall summaries, missing data statistics, and grouped summaries.
}
\examples{
library(dplyr)
library(gtsummary)

data(mtcars)
desc_all(
  data1 = mtcars,
  table_title = "Descriptive Summary of mtcars",
  quali = c("cyl", "gear"),
  quanti = c("mpg", "hp"),
  var_group = cyl,
  group_title = "Cylinders"
)

}
