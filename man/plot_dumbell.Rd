% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_dumbell.R
\name{plot_dumbell}
\alias{plot_dumbell}
\title{Plot a Dumbbell Chart for Adverse Events Analysis}
\usage{
plot_dumbell(
  df_soc_pt,
  df_pat_grp,
  df_pat_soc,
  ref_grp = NULL,
  colors_arm = c("#1b9e77", "#7570b3"),
  color_label = "Arm"
)
}
\arguments{
\item{df_soc_pt}{A data frame mapping system organ classes (SOC) to preferred terms (PT).
Must contain columns \code{soc} (System Organ Class) and \code{pt} (Preferred Term).}

\item{df_pat_grp}{A data frame of patient groups. Must contain columns \code{id_pat} (patient ID)
and \code{grp} (group assignment).}

\item{df_pat_soc}{A data frame linking patients to adverse event system organ classes.
Must contain columns \code{id_pat} (patient ID) and \code{soc} (System Organ Class).}

\item{ref_grp}{(Optional) A reference group for comparisons. Defaults to the first group in \code{df_pat_grp}.}

\item{colors_arm}{A vector of colors for the patient groups. Defaults to \code{c("#1b9e77", "#7570b3")}.}

\item{color_label}{A string specifying the legend label for the groups. Defaults to \code{"Arm"}.}
}
\value{
A \code{ggplot} object displaying the dumbbell chart.
}
\description{
This function creates a dumbbell plot comparing the occurrence of adverse events
across different patient groups. The plot includes the total number of adverse events,
the proportion of patients affected, and the risk difference with confidence intervals.
}
\examples{
df_soc_pt <- data.frame(
  soc = c("Arrhythmia", "Myocardial Infarction", "Pneumonia", "Sepsis"),
  pt = c("Cardiac Disorders", "Cardiac Disorders", "Infections", "Infections")
)

df_pat_grp <- data.frame(id_pat = paste0("ID_", 1:10),
                         grp = c(rep("A", 5), rep("B", 5)))

df_pat_soc <- data.frame(id_pat = c("ID_1", "ID_1", "ID_2", "ID_4", "ID_9"),
                         soc = c("Arrhythmia", "Myocardial Infarction",
                         "Arrhythmia", "Pneumonia", "Pneumonia"))

plot_dumbell(df_soc_pt, df_pat_grp, df_pat_soc)

}
